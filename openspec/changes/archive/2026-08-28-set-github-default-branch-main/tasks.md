## 1. Configuração da Branch Padrão no GitHub

- [x] 1.1 Alterar a branch padrão (*default branch*) do repositório no GitHub para `main` utilizando a CLI do GitHub (`gh repo edit --default-branch main`).
- [x] 1.2 Validar a configuração atualizada no GitHub (`gh repo view --json defaultBranchRef`).

## 2. Sincronização e Limpeza de Branches

- [x] 2.1 Verificar que a branch `main` local e remota estão perfeitamente sincronizadas com o commit mais recente (`93be511`).
- [x] 2.2 Excluir referências de branches remotas temporárias já integradas (`feature/extended-markdown-plugins`, `feature/add-mit-license`) do repositório remoto no GitHub.
